namespace TestTaskJun
{
    /// <summary>
    /// This structure contains routes for controllers.
    /// According to the condition of this task, there is only one route.
    /// </summary>
    public readonly struct Routes
    {
        public const string Mails = "api/mails";
    }
}